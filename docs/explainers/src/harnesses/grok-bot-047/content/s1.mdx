import { H2, H3, Peek, Term } from "../../../mdx-components"

<>
  <H2 id="s1" n="1">這棵樹是 0.47 反編譯稿</H2>
  <p>
    套件名在 asar 裡仍是 <code>sand</code>，產品名是 Grok Bot，版號 0.47.0，軌道
    <code>sandTrack: "stable"</code>。
    這份目錄不是 monorepo，也不是 0.18 那棵可編譯的重建稿。
    <Peek snip="p047">0.47 asar</Peek>
    <code>rhc</code> 是這份解說，不是 Grok Bot 的分支。
    <span className="stamp">2026-09-12</span>
  </p>
  <H3 id="s1-1" n="1.1">邊界釘在本機 Windows 安裝包</H3>
  <p>
    <code>PROVENANCE.md</code> 記錄安裝路徑
    <code>/mnt/c/Users/USER/AppData/Local/Programs/Grok Bot/</code>，以及
    <code>app.asar</code> SHA-256。
    抽出目錄在 <code>/mnt/e/eval-workspace/grok-bot-047/asar</code>。
    沒有對上這份 asar 或 VM zip 的行為，本文不寫成出廠事實。
  </p>
  <H3 id="s1-2" n="1.2">兩份 bytes：筆電 asar 與 VM host</H3>
  <p>
    筆電 asar 沒有 <code>dist/host/</code>。
    <code>source/host/</code> 來自 VM <code>studio-base-host-main.zip</code>，
    <code>sand-host/version</code> 是 <code>cb0ed63</code>，用 <code>// src/host/</code> 標記拆檔。
    筆電 CJS 被 minify，沒有那些標記，所以 electron-main 一側只留下 leftover 切片。
    0.18 重建稿當名字字典用，不當 0.47 的 bytes。
  </p>
  <H3 id="s1-3" n="1.3">行程根還在，renderer 原始碼不在</H3>
  <p>
    <code>source/</code> 仍按六個行程根擺：<code>electron-main</code>、
    <code>electron-preload</code>、<code>host</code>、
    <code>node-agent-coordinator</code>、<code>local-exec-daemon</code>、
    <code>box-exec-daemon</code>。
    後者在這兩份 zip 裡都缺。
    畫面原始碼不在 asar。出廠裝的是 Vite 編譯結果，
    <code>dist/renderer/assets/index-BhQ89Enc.js</code> 這類 hash chunk，沒有 <code>.map</code>。
  </p>
  <H3 id="s1-4" n="1.4"><code>frontend/</code> 是 leftover 路徑 stub</H3>
  <p>
    81 條 <code>/src/electron-renderer/features/...</code> 字串是 lazy-import 的 key，對上
    <code>chunk-view-*.js</code>。
    重建稿用這些路徑砌空檔，寫 <code>export &#123;&#125;</code>。
    那不是 0.18 那種可編輯的 Vite／React 重建。
  </p>
  <H3 id="s1-5" n="1.5">§2 到 §7 仍帶 0.18 章形，host 要對 VM 再引一次</H3>
  <p>
    送出、工具、沙箱、狀態那些章是從 0.18 解說搬來的。
    host 行為以 VM <code>cb0ed63</code> 為準。筆電側前綴已改成
    <code>dune-rpc</code>，見 <a className="xref" href="#s9">§9</a>。
    0.18 的「重建疊層」（Claude 路由器、本機 Docker、三支 DISABLE 旗標）不是這份 0.47 asar 的故事。
  </p>
</>
