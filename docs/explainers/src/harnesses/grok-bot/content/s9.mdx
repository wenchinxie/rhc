import { Peek, Term, H2, H3 } from "../../../mdx-components"

<>
      <H2 id="s9" n="9">port 怎麼插、事件怎麼跨行程</H2>
      <p>
        port 是呼叫端認得的方法名清單，通常附一份什麼都不做的預設。
        adapter 或 host extension 在開機時把真後端接上去。
        關掉遙測時呼叫端不必改。
        0.18 在 <code>host/ports/</code> 有七個檔。0.47 這份樹只收回兩個。
        <span className="stamp">2026-09-12</span>
      </p>

      <H3 id="s9-1" n="9.1">host/ports 收回兩檔</H3>
      <p>
        <code>product-analytics.ts</code> 只有 <code>createNoopSandProductAnalytics()</code>，回傳
        <code>trackEvent</code> 空函式。
        <Peek snip="t-pa-047">analytics noop</Peek>
        <code>sand-analytics-types.ts</code> 只有 <code>sandMessageLengthBucket</code>，依長度回 empty／xs／s／m／l／xl。
        沒有 <code>telemetry.ts</code>、<code>box.ts</code>、<code>transport.ts</code>、<code>mcp-state-executor.ts</code>、<code>user-computer.ts</code>。
      </p>

      <H3 id="s9-2" n="9.2">noop 先插上，HostTelemetryService 開機再換</H3>
      <p>
        <code>createNoopSandTelemetry()</code> 內聯在
        <code>source/host/extensions/box-store-sync/box-copy-in.ts</code>。
        它回傳 <code>startTurn</code>（得到 <code>NOOP_TURN</code> 的 setModel／setRequestId／finalize）以及一長串 <code>report*</code> 空方法。
        <Peek snip="t-noop-047">noop 本體</Peek>
        <code>TranscriptManager</code> 欄位預設就是這份物件。
        <Peek snip="t-tm-047">預設欄位</Peek>
        <code>loopback-sand-box.ts</code> 與 automations consumer 也用同一份工廠。
        host extension <code>id: "telemetry"</code> 的 <code>start</code> 才
        <code>new HostTelemetryService</code>，依賴 Auth、Experiments、Inference、Settings。
        <Peek snip="t-ext-047">接線</Peek>
      </p>

      <H3 id="s9-3" n="9.3">筆電側是另一個行程的 leftover</H3>
      <p>
        畫面看的不是 host port。preload 組 <code>dune-rpc</code> 加服務名與方法名的 channel，再
        <code>exposeInMainWorld("desktop")</code>。
        <Peek snip="dune-rpc">dune-rpc</Peek>
        coordinator 的 MessagePort 請求仍是 <code>sand:coordinator-port-request</code>，交付仍是
        <code>sand:coordinator-port</code>。
        <Peek snip="coord-port-047">埠名</Peek>
        桌面 leftover 在 <code>source/electron-main/telemetry/</code>，含 report pipe、structured log、send-ack。
        coordinator 有 <code>transport-stage-recorder</code>。local-exec 有 lifecycle sink。
        這些不跟 host 共用同一個緩衝區。
      </p>

      <H3 id="s9-4" n="9.4">錯誤表仍寫著 host/ports/telemetry.ts</H3>
      <p>
        <code>SAND-E0301</code> 的 <code>seededFrom</code> 字串是
        <code>SAND_BOX_BOOT_STAGES in host/ports/telemetry.ts</code>。
        <Peek snip="t-seeded">seededFrom</Peek>
        那份檔在 0.47 樹裡沒被拆出來。boot stage 字彙要以 leftover 與 host extension 為準，不能打開一個不存在的 <code>ports/telemetry.ts</code>。
      </p>

      <H3 id="s9-5" n="9.5">怎麼讀</H3>
      <p>
        問 host 回合與工具事件，打開 <code>source/host/extensions/telemetry/</code> 與 TranscriptManager 的插入點。
        問桌面 IPC 與 dune-rpc，打開 <code>extracted/electron-preload/preload.cjs</code> 與
        <code>source/electron-main/telemetry/</code>。
        問產品分析，打開 <code>host/ports/product-analytics.ts</code> 與
        <code>HostTelemetryService</code> 裡的 <code>SandProductAnalytics</code>。
        8.20 起的七個家族圖仍是 0.18 形狀。對 0.47 只當家族名目錄，細節以本節路徑為準。
      </p>
</>
